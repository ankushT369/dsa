#include <bits/stdc++.h>
using namespace std;



void f() {
    int n; string s; cin >> n >> s;
    if(s[0] = 'U' && s[1] == 'D' && s[s.size() - 1] = 'U')
        cout << "YES/";
    if(s[s.size() - 1] == 'D'
}



int main() {
    int t; cin >> t;
    while(t--) {
        f();
    }
    return 0;
}
