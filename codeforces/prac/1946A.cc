#include <bits/stdc++>
using namespace std;

#define ll long long

int main() {
    int t; cin >> t;

    while(t--) {
        int n; cin >> n;
        vector<ll> v;

        for(auto i = 0; i < n; i++) {
            int val; cin >> val;
            v.push_back(val);
        }
   













    }

    return 0;
}
